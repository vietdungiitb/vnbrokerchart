# Session Summary — 14/05/2026

**Gate cuối phiên:** widget contract PASS | viewport backfill tests PASS | type-check PASS | build:docs PASS | browser truthful-data smoke PASS | browser history-floor smoke PASS

---

## Hạng mục hoàn thành

### 1. VNInvest viewport sliding-window backfill

**Mục tiêu:** Khi pan chart cổ phiếu về trái ở mode VNInvest, hệ thống phải tự nạp thêm lịch sử theo timestamp đang nằm trong viewport thay vì tăng `days` neo về hiện tại.

**Thực hiện:**
- `VNBrokerChart` chuẩn hóa `VisibleRange.startDate/endDate` thành `Date` thật trong discontinuous scale.
- Thêm helper `src/demo/vniViewportBackfill.ts` để tính `fetchFrom/fetchTo` từ `visibleStart/visibleEnd`, `barCount`, và dữ liệu đã load.
- `LibraryShowcaseDemo` chuyển pan-to-load-more sang `VNInvestAdapter.fetchBars/fetchMoreBars`.
- Dùng `onVisibleDomainChange` làm tín hiệu edge-trigger khi pan đã chạm mép trái nhưng `VisibleRange` không thay đổi nữa.
- Preserve viewport hiện tại trước khi prepend lịch sử để tránh reset chart về range cũ.
- `webpack-dev-server` đổi sang full reload (`hot: false`, `liveReload: true`) để các tab demo không bị kẹt ở bundle cũ sau khi HMR abort.

### 2. Regression coverage

- Test contract widget xác nhận `VisibleRange` ở stock discontinuous mode không còn trả index số ra consumer.
- Test helper backfill xác nhận cửa sổ trượt chỉ mở khi gần mép trái và được neo theo timestamp trong viewport.

### 3. Truthful header and warmup follow-up

- `ChartCanvas`, `DynamicChart`, và `VNBrokerChart` được nối thêm callback `currentItem` để shell đọc đúng bar đang hover.
- OHLC strip của `LibraryShowcaseDemo` không còn lấy `lastBar` toàn cục; giờ ưu tiên `hovered bar`, fallback về `last visible bar` trong viewport.
- Nhãn `nến` trên strip chuyển sang `visibleRange.barCount`, nên chỉ còn phản ánh số bar thật trong khung nhìn.
- Live badge được phân nhánh theo source thật (`VNInvest` hoặc `Binance`).
- Helper backfill bổ sung `minTriggerBars` và demo truyền `vniWarmupBars` dựa trên indicator đang bật, để fetch sớm đủ warmup cho EMA/RSI/MACD ở mép trái.

### 4. Initial mount backfill and backend history floor

- `ChartCanvas` được vá để emit `VisibleRange` và domain ngay từ initial mount; trước đó path này chỉ phát ở `componentDidUpdate`, nên `VNInvest / 1Y` full-window không có tín hiệu ban đầu để schedule backfill.
- `LibraryShowcaseDemo` được bổ sung retry backfill sau khi `vniLoading` chuyển sang `false` và reset cờ exhausted khi nạp dataset VNI mới.
- Demo shell thêm badge truthful `Lịch sử khả dụng từ 18/04/2025` khi backend xác nhận không còn daily bar cũ hơn cho `VCB`.
- Probe runtime xác nhận đây là trần dữ liệu backend, không còn là lỗi trigger của frontend:
  - native `/core/v1/market-data/daily-prices/VCB/?date_from=2022-11-12&date_to=2025-04-17` trả `count = 0`
  - legacy `/api/stock-management/stocks/VCB/chart/?days=1200&timeframe=D` trả `count = 264`, `minDate = 2025-04-18T00:00:00`

---

## Browser smoke

- URL: `http://127.0.0.1:3000/`
- Source: `VNInvest`
- Symbol: `VCB`
- Timeframe: `D`
- Range: `1M`
- Kết quả: pan-to-load-more hoạt động; bar count tăng `82 nến -> 256 nến`.
- Evidence mạng: phát sinh 3 request `/core/v1/market-data/daily-prices/VCB/` với các cửa sổ lịch sử lùi dần:
  - `2025-10-20 -> 2026-01-07`
  - `2025-07-28 -> 2025-10-19`
  - `2025-05-05 -> 2025-07-27`
- Follow-up truthful smoke: sau nhiều lần pan trái tiếp tục, strip giữ `19 nến` theo viewport, live badge giữ `● LIVE · VNInvest`, OHLC strip khớp bar ngày `2025-09-12`, và tooltip indicator cho cùng bar trả `EMA(20)`, `EMA(50)`, `RSI(14)`, `MACD` với giá trị số thay vì `n/a`.
- History-floor smoke: `VNInvest / VCB / 1Y` sau reload bắn 4 request `daily-prices` ngay lúc mount và topbar hiển thị `Lịch sử khả dụng từ 18/04/2025`.

---

## Files đã sửa

- `src/widget/VNBrokerChart.tsx`
- `src/widget/__tests__/VNBrokerChart.contract.test.tsx`
- `src/lib/ChartCanvas.tsx`
- `src/lib/core/DynamicChart.tsx`
- `src/demo/vniViewportBackfill.ts`
- `src/demo/__tests__/vniViewportBackfill.test.ts`
- `src/demo/LibraryShowcaseDemo.tsx`
- `src/demo/i18n.tsx`
- `src/demo/demo.css`
- `config/webpack.config.js`
- `module_tree_full.md`
- `docs/upgrade-standard/AUDIT_LEDGER.md`
- `quality/results/session-2026-05-14.md`

---

## Validation commands

- `npm test -- src/demo/__tests__/vniViewportBackfill.test.ts src/widget/__tests__/VNBrokerChart.contract.test.tsx`
- `npm run type-check`
- `npm run build:docs`
- `python scripts/generate_module_tree.py`

---

## Residual risk

- Daily history backfill, truthful header, và history-floor smoke đã pass. Tuy nhiên với `VCB`, cả native daily endpoint lẫn legacy chart endpoint hiện chỉ có dữ liệu từ `18/04/2025`; muốn có nến cũ hơn cần backend ingest hoặc endpoint lịch sử khác. Intraday VNInvest sâu hơn vẫn bị chặn bởi native history endpoint chưa hoàn thiện.